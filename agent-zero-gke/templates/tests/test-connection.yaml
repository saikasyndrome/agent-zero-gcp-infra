apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "agent-zero.fullname" . }}-test-connection"
  labels:
    {{- include "agent-zero.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "agent-zero.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
